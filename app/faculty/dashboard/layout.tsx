import FacultyShell from "@/app/faculty/FacultyShell";
export default function FacultyDashboardLayout({ children }: { children: React.ReactNode }) {
  return <FacultyShell>{children}</FacultyShell>;
}
